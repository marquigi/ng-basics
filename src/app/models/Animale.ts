// La definizione di un dato tipo custom, si può fare:
// type, interface, class

// type
export type Animale = {
    nome:string;
    razza:string;
}

// interface
export interface IAnimale {
    nome:string;
    razza:string;
}
